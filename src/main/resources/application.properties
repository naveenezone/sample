spring.application.name=sample

# === MySQL datasource ===
# Update username/password as appropriate for your local MySQL instance
spring.datasource.url=jdbc:mysql://192.168.1.105:3306/clinicals
spring.datasource.username=root
spring.datasource.password=mysqlrootpwd
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA/Hibernate ===
# Do not auto-create or update schema; use existing tables from clinicals.sql
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.open-in-view=false
# spring.jpa.properties.hibernate.jdbc.time_zone=UTC
